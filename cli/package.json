{
  "name": "@bamboo/cli",
  "version": "0.1.0",
  "description": "Bamboo CLI — 命令行入口（chat / run / serve）",
  "type": "module",
  "bin": {
    "bamboo": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@bamboo/bridge": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
